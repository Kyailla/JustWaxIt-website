import Link from 'next/link';import type { Metadata } from 'next';import { posts } from '@/lib/site';export const metadata:Metadata={title:'Waxing Blog Twin Falls',description:'Waxing, brow, lash, and beauty education articles from JustWaxIt in Twin Falls, Idaho.'};export default function Page(){return <main className="px-4 py-20"><div className="mx-auto max-w-5xl"><h1 className="font-display text-5xl gold">The JustWaxIt Blog</h1><div className="mt-8 grid gap-5 md:grid-cols-3">{posts.map(p=><Link href={`/blog/${p.slug}`} key={p.slug} className="card rounded-3xl p-6"><p className="text-sm gold">{new Date(p.date).toLocaleDateString()}</p><h2 className="mt-3 font-display text-2xl">{p.title}</h2><p className="mt-3 text-white/70">{p.excerpt}</p></Link>)}</div></div></main>}
